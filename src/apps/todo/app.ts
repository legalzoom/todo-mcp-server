import type { AppDefinition } from '../registry.js';

export const todoApp: AppDefinition = {
  title: 'Todo Admin',
  description: 'Open an administrative interface for managing Todos (list, create, update, delete)',
  htmlFile: 'index.html',
};
